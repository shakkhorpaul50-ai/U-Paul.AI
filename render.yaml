services:
  - type: web
    name: u-paul-ai
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: RELEASE_URL
        value: SET_TO_GGUF_RELEASE_URL
      - key: MODEL_PATH
        value: models/gguf/smol260M-Q3_K_M.gguf
      - key: N_CTX
        value: 512
      - key: N_BATCH
        value: 4
